# Pinned: PaddlePaddle/PaddleOCR change model defaults between minors (3.7
# changed its default models), and an unpinned rebuild would silently
# change what text comes out of the same photo.
paddlepaddle==3.3.1
paddleocr==3.7.0
pypdfium2==5.13.0
flask>=3.0,<4
pillow>=11
